generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model User {
  id BigInt @id @default(autoincrement())
}

model School {
  id BigInt @id @default(autoincrement())
}

model Member {
  id BigInt @id @default(autoincrement())
}